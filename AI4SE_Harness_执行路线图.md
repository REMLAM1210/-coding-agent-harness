# AI4SE 期末项目执行路线图（A · Coding Agent Harness）

> 本文档 = 通用要求 + A 文件的操作化拆解。按阶段顺序推进，每个阶段末尾有"完成判定"，达标再进入下一阶段。**在 SPEC.md 与 PLAN.md 完成并通过冷启动验证之前，不要写任何实现代码。**

---

## 阶段 0：准备工作（开工前必须敲定）

- [ ] **选定编码智能体**：Claude Code / OpenAI Codex CLI / Cursor Agent / OpenCode / GitHub Copilot CLI / Gemini CLI 任一，按 Superpowers 文档安装插件（https://github.com/obra/superpowers）。
- [ ] **选定实现语言**：不限语言，但要考虑：
  - 是否方便写一个"可注入 mock 的 LLM 抽象层"（几乎所有语言都行，但 Python/TypeScript/Go 生态里 mock/stub 写单测最省事）。
  - 是否方便做凭据安全存储（系统钥匙串绑定库是否成熟，如 Python `keyring`、Node `keytar`/`@napi-rs/keyring`、Go `zalando/go-keyring`）。
- [ ] **想清楚"两顶帽子"的界线**（A.4 节的核心纪律）：
  - 开发时可以尽情用 Superpowers/编码智能体的 subagent、Skill、hooks、memory 帮你**写代码**。
  - 但你**交付的 harness 内核本身**——主循环、工具分发、治理护栏、反馈回灌——必须是你自己代码里跑的逻辑，不能架在 LangChain AgentExecutor / AutoGen / CrewAI / LlamaIndex agent / 某编码智能体自带 agent runner 之上。这条线要在动笔前就想清楚，后面才不会返工。
- [ ] **建好 Git 仓库**（NJU Git，公开或私有+助教协作者）。

**完成判定**：能用一句话跟别人说清楚"我打算用什么语言、通过哪个智能体+Superpowers 来做这个 harness"。

---

## 阶段 1：Brainstorming → SPEC.md

启动智能体，触发 `brainstorming` 技能，让它主动追问你。你的角色是"质询"而不是"领受指令"——任何它含糊带过的地方都要逼它说清楚。

SPEC.md 必须包含通用要求 §4.2 的 10 项，**外加 A 文件要求的额外一节「领域与机制设计」**：

### 通用 10 项（§4.2）
1. 问题陈述（要解决什么问题、目标用户、为什么值得做）
2. 至少 5 个用户故事（INVEST 原则）
3. 功能规约（按模块拆，每项含输入/行为/输出/边界条件/错误处理）
4. 非功能性需求（性能、**安全含凭据威胁模型**、可用性、可观测性）
5. 系统架构（组件图、数据流、外部依赖）
6. 数据模型
7. 凭据与分发设计
8. 技术选型与理由
9. 验收标准
10. 风险与未决问题

### 额外一节：领域与机制设计（A.5，必须逐条回答）
- coding 领域的**反馈信号**是什么？（通常：测试/lint/类型检查的结果）
- **危险动作**是什么、边界怎么划？（通常：删库、危险 shell 命令、对外发布）
- 需要哪些**工具/动作**？（读写文件、执行 shell、跑构建与测试……）
- 需要什么**记忆**、跨会话保留什么、怎么按需检索而不是全量塞进上下文？
- 你选哪个维度做**主要贡献**（治理 / 反馈闭环 / 工具分发与多 agent 编排 / 记忆，见下方"选重点维度"）？为什么？
- 这些机制打算怎么落成代码（呼应 A.4 的判定标准）？

### 关键决策点：选重点维度
六个维度（决策/工具/记忆/治理/反馈/配置）都要有能跑通的**最低实现**，但必须选一个做深：

| 维度 | 做深的典型内容 |
|---|---|
| 治理 | 护栏规则引擎、沙箱、HITL 审批状态机、动作范围围栏 |
| 反馈闭环 | 确定性校验器（测试/lint/类型检查解析）、失败分类、多轮自我修正循环 |
| 扩展 | 工具分发机制、多 agent 编排 |
| 记忆/上下文工程 | 自己实现存储与检索（不能直接用框架自带 memory） |

文件建议优先选**治理**或**反馈闭环**——它们天然是代码而非提示词，最容易同时满足 A.4 的 (A)(B)(C) 三条判据。

**完成判定**：SPEC.md 每一节都写完，且你能对着"移除真实 LLM 后这个机制还能不能被单测验证"这条标准，逐个检查你设计的机制。

---

## 阶段 2：writing-plans → PLAN.md

把签字确认后的设计，用 `writing-plans` 技能拆成 task 列表：

- 每个 task 颗粒度：2–5 分钟能完成，路径明确，验证步骤明确。
- 每个 task 写清：目标、涉及文件、实现要点、**验证步骤（含要写的失败测试）**。
- 显式标出依赖关系与可并行部分（为后面的 worktree 并行做准备）。

**完成判定**：任何一个 task 单独拎出来，一个 subagent 不需要额外提问就能在一次会话内做完。

---

## 阶段 3：SPEC_PROCESS.md（边做边记）

从阶段 1 开始就要同步记录，不要等到最后补：

- brainstorming 阶段智能体追问了哪些好问题、哪些让你改了原设计。
- 至少 3 轮关键迭代的对话节选 + 你的处理决策。
- 哪些建议你采纳了、哪些你推翻/修正了，为什么。
- 一段反思：这个技能哪里做得好、哪里让你不满意。

---

## 阶段 4：冷启动验证（正式写代码前的强制关卡）

这是全流程里最容易被跳过、但文件里反复强调"最关键的客观证据"的一步：

1. 换一个**类型不同**的编码智能体（比如主开发用 Claude Code，这里就换 Codex/Cursor/Gemini CLI 等）。
2. 开全新 session，**不导入**任何先前对话或 memory。
3. 只给它 `SPEC.md` + `PLAN.md`，不做任何口头补充解释。
4. 指定它从 PLAN 里挑 1–2 个 task 自主推进（约 1–2 小时），并明确告诉它："遇到不确定的地方就暂停提问，不要靠猜测继续"。
5. 记录到 SPEC_PROCESS.md：它在哪卡住提问？暴露了 SPEC 的什么缺陷？它的理解和你原意哪里不一致（是 spec 写错还是它读错）？产出和预期差多少？你据此怎么修订了 SPEC/PLAN（给出修订前后的关键 diff）？

**完成判定**：SPEC/PLAN 已经根据这次冷启动的反馈修订过，且这个过程被完整记录在案。

---

## 阶段 5：实现（正式开工）

按通用要求 §4.6 的流程，同时死守 A.4 的红线：

1. **git worktrees**：每个独立功能/大模块一个 worktree，对应一个 PR。
2. **subagent 驱动开发**：每个 task 派新鲜 subagent 完成。
3. **TDD 强制**：先写失败测试（红）→ 最少实现让它变绿 → 重构。不允许先写实现再补测试。
4. **两阶段评审**：每个 task 完成后，先查 spec 合规，再查代码质量；critical issue 必须修复才能进下一个 task。
5. **AGENT_LOG.md 持续记录**：每条含时间戳+task 编号、触发的 Superpowers 技能、关键 prompt/context 配置、subagent 输出关键片段或 commit hash、人工干预内容与原因、学到的教训。
6. **PLAN.md 持续更新**：每完成一个 task 就标记完成并附 commit hash。

### 实现时对照 A.4 的自查清单（针对 harness 内核部分）
- [ ] agent 主循环（组织上下文→调用 LLM→解析动作→分发执行→回灌结果→停机判断）是自己写的，没有架在 AgentExecutor/AutoGen/CrewAI/LlamaIndex agent/某 SDK 自带 runner 之上。
- [ ] 有一个可替换为 mock 的 LLM 抽象层，且真的能在离线（无网络、无真实 LLM）下跑。
- [ ] 危险动作拦截是一个函数（如 `guardrail(action)`），不是系统提示词里的一句话。
- [ ] 反馈信号是一个校验器/传感器（解析产物→客观判定→回灌），不是"让 LLM 自己检查"。
- [ ] 对每个核心机制自问：把真实 LLM 换成 mock/stub 后，这个机制还能用确定性单测验证吗？能——才算数；不能——它本质是提示词，不计入 harness 实现工作量。
- [ ] 配置文件/规则文件/Skill/提示词文件本身不算 harness 实现工作量，只是"内容物"。

**完成判定**：六个维度都有可运行的最低实现，其中一个维度（治理/反馈闭环/扩展/记忆）有明显更深的实现深度。

---

## 阶段 6：mock-LLM 单测 + 机制演示（A.6 额外要求）

- [ ] harness 核心机制（工具分发、治理拦截、反馈回灌、记忆读写、停机）都有 mock/stub LLM 驱动的确定性单测，**不依赖网络、不依赖真实 LLM**。
- [ ] 提交一个"机制演示"（测试用例或可重复运行脚本），在 mock LLM 下确定性复现：
  1. 治理护栏拦截一个危险动作；
  2. 注入一次失败 → 反馈闭环让 agent 拿到反馈并据此改变下一步动作；
  3. 你重点维度的一个确定性行为（呼应阶段 1 里选的"主要贡献"）。

---

## 阶段 7：凭据安全（§3.1，全程必做，不是最后补）

- [ ] key 不硬编码、不进 Git（含历史）、不进日志/终端 history/明文配置文件。
- [ ] 至少一种安全存储：系统钥匙串 / 密钥管理服务 / 带主密码的加密文件。若用环境变量，要通过 `.env` 加载而非 `export`，并在 SPEC 里说明明文风险。
- [ ] 首次运行能引导用户安全录入 key（隐藏输入），能查看/更新/清除（查看状态不回显明文）。
- [ ] SPEC 安全一节写清凭据威胁模型与对策。

---

## 阶段 8：分发 + CI/CD + 云部署

- [ ] 分发形态任选：容器镜像（`docker build`+`docker run` 单命令启动，推送公开 registry）/ 原生二进制（说明平台架构、签名情况、首次运行拦截处理）/ 包管理器（npm/PyPI/cargo/Homebrew，给出安装命令）。
- [ ] README 写清：获取方式、运行命令、**key 在目标机器上如何安全配置**、已知限制。
- [ ] CI（`.gitlab-ci.yml`）配置，**必须包含名为 `unit-test` 的 job**，每次 push 自动跑测试；选容器则 CI 也要构建镜像。
- [ ] 云部署（可选服务端项目）：Vercel/Render/Railway/Fly.io/阿里云/腾讯云等，控制成本优先用免费额度。
- [ ] **最终必须有一个可访问的线上 WebUI 地址**（这是硬性交付项）。

---

## 阶段 9：收尾文档与交付清单核对

最终交付清单（通过同一个 NJU Git 仓库链接提交）：

- [ ] `SPEC.md`、`PLAN.md`、`SPEC_PROCESS.md`
- [ ] 完整源代码，规范 commit/PR 历史（拒绝单次 commit 提交全部代码），仓库内无任何真实凭据；源码须包含**你自己实现的 harness 内核 + mock-LLM 单元测试**
- [ ] 分发产物与说明（`Dockerfile` 或二进制构建脚本/打包配置）
- [ ] `README.md`（项目简介、安装、运行、分发命令、目录结构、安全边界说明 —— 这几节必须都有）
- [ ] `AGENT_LOG.md`
- [ ] CI 配置（含 `unit-test` job），且**最后一次 CI/CD 执行必须是 pass 状态**
- [ ] `REFLECTION.md`（1500–2500 字，**必须学生本人撰写**，可用 AI 辅助润色但需标注）
- [ ] 线上部署 URL（WebUI 可访问）
- [ ] §A.6 的机制演示

### REFLECTION.md 建议覆盖的问题（§五）
- 哪些 Superpowers 技能发挥最大作用、哪些"形式大于实质"？
- TDD 强制在 AI 协作下是阻碍还是放大器？
- subagent-driven 工作流让智能体能自主运行多久而不偏离主题？什么 task 颗粒度最优？
- SPEC/PLAN 质量如何影响实现质量（举一个"规约不清导致 subagent 偏离"的具体案例）？
- 你最有效的 prompt/context 策略是什么、为什么有效？
- 凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？
- 如果重做你会改变什么？
- 你对 Superpowers 方法论的批判——它假设了什么，这些假设在你的项目里成立吗？

---

## 学术规范提醒

- 你认为更适合自己手写的代码（如核心算法），在文件/函数顶部注释说明。
- 第三方代码要遵守许可证，并在 README 列出。
- REFLECTION.md 必须本人撰写，不能让 AI 代写。

---

## 你现在最需要先做的三件事

1. 确定编码智能体 + 实现语言，装好 Superpowers 插件。
2. 想清楚"重点维度"打算选治理还是反馈闭环（或扩展/记忆），这个决定会贯穿 SPEC、实现、机制演示三个阶段。
3. 启动 `brainstorming` 技能，开始产出 SPEC.md——记得同步开始写 SPEC_PROCESS.md，不要等到最后回忆。
